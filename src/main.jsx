import ReactDOM from "react-dom/client";
import App from "./App.jsx";
import axios from "axios";

// const promise = axios.get("http://localhost:3005/persons");
// console.log(promise);

ReactDOM.createRoot(document.getElementById("root")).render(<App />);
